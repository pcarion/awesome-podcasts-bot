name: 'awsome-podcast-bot-action'
description: 'Github Action to help maintaining a directory of podcasts'
inputs:
  repo-token:
    description: 'Github personal access token to generate the podcast file'
    required: true
runs:
  using: 'node12'
  main: 'action/index.js'
